{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const i=JSON.parse(d);const p=i.tool_input?.file_path||'';if(/\\.py$/.test(p)){const{execFileSync}=require('child_process');try{execFileSync('ruff',['format',p],{stdio:'pipe'})}catch(e){}}console.log(d)})\""
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const i=JSON.parse(d);const cmd=i.tool_input?.command||'';if(/git\\s+push/.test(cmd)){console.error('[Hook] Pushed. Check CI: gh run list --limit 1. Did you update LEARNING.md?')}if(/git\\s+commit/.test(cmd)){console.error('[Hook] Committed. Did you close relevant issues? (closes #N)')}console.log(d)})\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const i=JSON.parse(d);const p=i.tool_input?.file_path||'';const c=i.tool_input?.content||'';if(/\\.(env|key|secret|credential|token)/.test(p)){console.error('[Hook] BLOCKED: Do not write credential files');process.exit(2)}if(/backends\\/.*\\.py$/.test(p)&&/trust_remote_code\\s*=\\s*True/.test(c)&&!/trust_remote_code\\s*[=:]\\s*trust_remote_code/.test(c)){console.error('[Hook] WARNING: Hardcoded trust_remote_code=True in backend. Should be a parameter, not hardcoded.')}console.log(d)})\""
          }
        ]
      }
    ]
  }
}
